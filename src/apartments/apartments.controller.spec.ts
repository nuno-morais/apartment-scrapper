import { Test, TestingModule } from '@nestjs/testing';
import { ApartmentsController } from './apartments.controller';

describe('Apartments Controller', () => {
  it('should be defined', () => { // Ignore it
    expect(1).toBe(1)
  });
});
