import { CreateLinkInteractor } from './createLink.interactor';

describe('CreateLinkInteractor', () => {
  it('should be defined', () => {
    // expect(new CreateLinkInteractor()).toBeDefined();
  });
});
